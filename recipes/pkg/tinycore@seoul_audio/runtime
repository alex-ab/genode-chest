runtime | ram: 2450M | caps: 1350 | binary: init

+ requires
  + file_system | writeable: yes | label: vdi
  + nic
  + rom | label: platform_info
  + gui
  + rtc
  + vm
  + usb
  + rm
  + timer
  + report | label: shape
  + play

+ content
  + rom | label: vm_seoul_audio.cfg
  + rom | label: init
  + rom | label: vfs.lib.so
  + rom | label: vfs_import.lib.so
  + rom | label: ld.lib.so
  + rom | label: vdi_block
  + rom | label: tc16.vdi
  + rom | label: seoul

+ config | verbose: no
  + parent-provides
  | + service ROM
  | + service PD
  | + service CPU
  | + service LOG
  | + service VM
  | + service RM
  | + service Gui
  | + service Rtc
  | + service Nic
  | + service Timer
  | + service Report
  | + service File_system
  | + service Usb
  | + service Play
  + start vdi_block | ram: 8M | caps: 100
  | + provides
  | | + service Block
  | + config | file: /tc16.vdi | writeable: yes
  | | + vfs
  | |   + fs | label: vdi | buffer_size: 2M
  | |   + import | overwrite: no
  | |     + rom tc16.vdi | binary: yes
  | + route
  |   + service File_system | label: vdi
  |   | + parent | identity: vdi | resource: /
  |   + any-service
  |     + parent
  + start seoul | ram: 16G | caps: 5000
    + binary seoul
    + route
      + service ROM | label: platform_info
      | + parent | label: platform_info
      + service ROM | label: config
      | + parent | label: vm_seoul_audio.cfg
      + service Block | label: disk0
      | + child vdi_block
      + service Report | label: shape
      | + parent | label: shape
      + service Gui | label: fb1.0
      | + parent | label:
      |   . XXX required for window/shape/pointer matching - does not work for other fbX.Y
      + any-service
        + parent
-
