runtime | ram: 2514M | caps: 1390 | binary: init
+ requires
  + nic
  + rom | label: platform_info
  + gui
  + rtc
  + vm
  + usb
  + rm
  + timer
  + report | label: shape
+ content
  + rom | label: vm_seoul_usb.cfg
  + rom | label: tc-firefox.raw
  + rom | label: init
  + rom | label: vfs_block
  + rom | label: vfs.lib.so
  + rom | label: vfs_import.lib.so
  + rom | label: ld.lib.so
  + rom | label: seoul
+ config | verbose: no
  + parent-provides
  | + service ROM
  | + service PD
  | + service CPU
  | + service LOG
  | + service VM
  | + service RM
  | + service Gui
  | + service Rtc
  | + service Nic
  | + service Timer
  | + service Report
  | + service Usb
  + start vfs_block | caps: 300 | ram: 404M
  | + provides
  | | + service Block
  | + config
  | | + vfs
  | | | + ram
  | | | + import
  | | |   + rom tc-firefox.raw
  | | + default-policy
  | |                  file:       /tc-firefox.raw
  | |                  writeable:  yes
  | |                  block_size: 512
  | + route
  |   + any-service
  |     + parent
  + start seoul | caps: 5000 | ram: 16G
    + binary seoul
    + route
      + service ROM    | label: platform_info | + parent | label: platform_info
      + service ROM    | label: config        | + parent | label: vm_seoul_usb.cfg
      + service Block                         | + child vfs_block
      + service Report | label: shape         | + parent | label: shape
      + service Gui    | label: fb1.0         | + parent | label: fb1.0
      + any-service                           | + parent
-
